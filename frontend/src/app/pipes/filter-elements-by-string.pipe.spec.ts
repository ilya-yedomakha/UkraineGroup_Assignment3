import { FilterElementsByStringPipe } from './filter-elements-by-string.pipe';

describe('FilterElementsByStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterElementsByStringPipe();
    expect(pipe).toBeTruthy();
  });
});
