import { FilterElementsByNumbersPipe } from './filter-elements-by-numbers.pipe';

describe('FilterElementsByNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterElementsByNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
